(function () {
	'use strict';
	angular.module('support', [
		'routes'
	]);

	/* @ngInject */
	function config() {

	}

	/* @ngInject */
	function run() {
		
	}

	angular.module('support').config(config);
	angular.module('support').run(run);
})();
