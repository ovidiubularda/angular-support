(function () {
	'use strict';
	angular.module('sample', [
		'routes'
	]);

	/* @ngInject */
	function config() {

	}

	/* @ngInject */
	function run() {

	}

	angular.module('sample').config(config);
    angular.module('sample').run(run);
})();
